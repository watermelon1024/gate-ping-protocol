# Supported protocol versions
# Note: Must be sorted by 'number' in ascending order, otherwise display issues may occur
protocols:
  # number: Protocol version number
  # See https://minecraft.wiki/w/Minecraft_Wiki:Projects/wiki.vg_merge/Protocol_version_numbers
  # names: Array of display names (optional). If left empty, names will be retrieved from Gate's internal data. 
  # If not found, fallback name will be "v<number>".
  - number: 771
    names: ["1.21.6"]
  - number: 772
    names: ["1.21.7", "1.12.8"]
